{
  "body": [
    {
      "type": "text",
      "content": "Most React developers reach for <code>useEffect</code> as reflexively as they reach for <code>useState</code>. And most of them — myself included, for longer than I'd like to admit — don't fully understand when it runs, how many times it runs, and what the cost of each of those runs actually is."
    },
    {
      "type": "text",
      "content": "After profiling several production apps this year, I found that misused effects were responsible for some of the most subtle performance bugs I've ever debugged. Here's what I learned."
    },
    {
      "type": "heading",
      "level": 2,
      "content": "Effects Run After the Browser Paints"
    },
    {
      "type": "text",
      "content": "This is the single most important thing to internalize: <strong>useEffect runs after the browser commits the DOM and paints the new frame.</strong> That means if you <code>setState</code> inside an effect, you're always paying for at least one extra render — and the user may briefly see the old state."
    },
    {
      "type": "code",
      "lang": "jsx",
      "content": "// Anti-pattern: derived state in useEffect\nfunction UserCard({ user }) {\n  const [fullName, setFullName] = useState('');\n\n  useEffect(() => {\n    setFullName(user.firstName + ' ' + user.lastName);\n  }, [user]);\n\n  return <h2>{fullName}</h2>;\n}"
    },
    {
      "type": "text",
      "content": "This component renders <strong>twice</strong> on every prop change: once with the stale <code>fullName</code>, then a commit, then the effect fires, then a second render. The fix is to just compute it during render:"
    },
    {
      "type": "code",
      "lang": "jsx",
      "content": "function UserCard({ user }) {\n  const fullName = user.firstName + ' ' + user.lastName;\n  return <h2>{fullName}</h2>;\n}"
    },
    {
      "type": "heading",
      "level": 2,
      "content": "The Dependency Array Is a Contract"
    },
    {
      "type": "text",
      "content": "When you write <code>[user]</code>, you're promising React that the effect only depends on <code>user</code>. If you lie — if the effect closes over some other state or prop that isn't listed — you get stale closures that silently serve outdated data. ESLint's <code>exhaustive-deps</code> rule exists because humans are terrible at keeping this contract in their heads."
    },
    {
      "type": "quote",
      "content": "Every effect should describe a single, self-contained synchronization with something outside of React."
    },
    {
      "type": "heading",
      "level": 2,
      "content": "When You Actually Need It"
    },
    {
      "type": "list",
      "ordered": true,
      "items": [
        "Subscribing to an external store (WebSocket, event emitter, browser API)",
        "Syncing state to <code>localStorage</code>, URL, or another side channel",
        "Triggering analytics events tied to lifecycle, not user actions",
        "Running imperative code on a DOM node (focus, scroll, measure)"
      ]
    },
    {
      "type": "text",
      "content": "Notice what's not on this list: transforming props, computing derived values, or chaining state updates. Those belong in render, in a <code>useMemo</code>, or in an event handler — never in an effect."
    },
    {
      "type": "heading",
      "level": 2,
      "content": "The Mental Model That Fixed Everything"
    },
    {
      "type": "text",
      "content": "I stopped thinking of <code>useEffect</code> as \"run this after render.\" I started thinking of it as \"keep this external thing in sync with my component's state.\" Once I made that switch, the rule for when to use it became obvious: if there's no external thing, there shouldn't be an effect."
    },
    {
      "type": "divider"
    },
    {
      "type": "text",
      "content": "If you want to go deeper, the React docs' <em>You Might Not Need an Effect</em> page is one of the best pieces of technical writing the React team has ever shipped. Read it twice."
    }
  ]
}
